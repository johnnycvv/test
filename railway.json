{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd api && npm install --production=false"
  },
  "deploy": {
    "startCommand": "cd api && node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
